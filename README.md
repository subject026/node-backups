# Node Backups

## config

```json
{
  "backupLocation": "/test-output",
  // list of specific files and folders to backup
  "backupsList": ["~/some-project"],
  // backup each folder contained within these folders as an individual archive
  "contentsOf": ["/dev", "/learn"]
}
```
